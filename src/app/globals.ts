export class Globals {
  //Set to false if only the minimal voting tool is required
  static advancedMode : boolean = false;
  static globalNumberOfVoters : number = 1;
  static resultLotteries: number[]; // Used for coloring only
}
